overwrite: true
schema:
  # list your schema endpoints here
  - {GRAPHQL_ENDPOINT}
documents: "**/*.graphql"
generates:
  src/graphql/generated.ts:
    plugins:
      - typescript
      - typescript-operations
